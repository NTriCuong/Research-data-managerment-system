# Integration Tests

Contains tests that exercise multiple backend layers with a test database.

Set `TEST_DATABASE_URL` before running these tests.

Current coverage includes:

- staging metadata upload and submit behavior
- CRUD for departments, output types, research domains, researchers, and keywords
- the complete Super Admin user-management lifecycle
